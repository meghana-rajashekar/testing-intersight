
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.2
  creationTimestamp: null
  name: clusterprofiles.kubernetes.intersight.jet.crossplane.io
spec:
  group: kubernetes.intersight.jet.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - intersightjet
    kind: ClusterProfile
    listKind: ClusterProfileList
    plural: clusterprofiles
    singular: clusterprofile
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterProfile is the Schema for the ClusterProfiles API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ClusterProfileSpec defines the desired state of ClusterProfile
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource.
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accountMoid:
                    description: The Account ID for this managed object.
                    type: string
                  aciCniProfile:
                    description: A reference to a kubernetesAciCniProfile resource.
                      When the $expand query parameter is specified, the referenced
                      resource is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  action:
                    description: User initiated action. Each profile type has its
                      own supported actions. For HyperFlex cluster profile, the supported
                      actions are -- Validate, Deploy, Continue, Retry, Abort, Unassign
                      For server profile, the support actions are -- Deploy, Unassign.
                    type: string
                  actionInfo:
                    description: No longer updated, to see action details such as
                      name of the action performed, status, failure reason, etc. look
                      at the ConfigContext and at details of the related workflowInfo.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        failureReason:
                          description: No longer maintained and will be removed soon.
                          type: string
                        name:
                          description: Name of the Action performed on a resource
                            like VM, Disk etc.
                          type: string
                        objectType:
                          description: The fully-qualified name of the instantiated,
                            concrete type. The value should be the same as the 'ClassId'
                            property.
                          type: string
                        status:
                          description: No longer maintained and will be removed soon.
                            * `None` - A place holder for the default value. * `InProgress`
                            - Action triggered on the resource is still running. *
                            `Success` - Action triggered on the resource is completed
                            successfully. * `Failure` - Action triggered on the resource
                            is failed.
                          type: string
                      type: object
                    type: array
                  actionParams:
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        name:
                          description: The action parameter identifier.
                          type: string
                        objectType:
                          description: The fully-qualified name of the instantiated,
                            concrete type. The value should be the same as the 'ClassId'
                            property.
                          type: string
                        value:
                          description: The action parameter value.
                          type: string
                      type: object
                    type: array
                  additionalProperties:
                    type: string
                  ancestors:
                    description: An array of relationships to moBaseMo resources.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  associatedCluster:
                    description: A reference to a kubernetesCluster resource. When
                      the $expand query parameter is specified, the referenced resource
                      is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  certConfig:
                    description: Certificates and keys that are used to configure
                      a Kubernetes cluster. If user does not specify any Certificates
                      or Keys, system generated certificates will be used to configure
                      the cluster.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        caCert:
                          description: Certificate for the Kubernetes API server.
                          type: string
                        caKey:
                          description: Private Key for the Kubernetes API server.
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        etcdCert:
                          description: Certificate for the etcd cluster.
                          type: string
                        etcdEncryptionKey:
                          items:
                            type: string
                          type: array
                        etcdKey:
                          description: Private key for the etcd cluster.
                          type: string
                        frontProxyCert:
                          description: Certificate for the front proxy to support
                            Kubernetes API aggregators.
                          type: string
                        frontProxyKey:
                          description: Private key for the front proxy to support
                            Kubernetes API aggregators.
                          type: string
                        objectType:
                          description: The fully-qualified name of the instantiated,
                            concrete type. The value should be the same as the 'ClassId'
                            property.
                          type: string
                        saPrivateKey:
                          description: Service account private key used by Kubernetes
                            Token Controller to sign generated service account tokens.
                          type: string
                        saPublicKey:
                          description: Service account public key used by Kubernetes
                            API Server to validate service account tokens generated
                            by the Token Controller.
                          type: string
                      type: object
                    type: array
                  classId:
                    description: The fully-qualified name of the instantiated, concrete
                      type. This property is used as a discriminator to identify the
                      type of the payload when marshaling and unmarshaling data.
                    type: string
                  clusterIpPools:
                    description: An array of relationships to ippoolPool resources.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  configContext:
                    description: The configuration state and results of the last configuration
                      operation.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        configState:
                          description: Indicates a profile's configuration deploying
                            state. Values -- Assigned, Not-assigned, Associated, Pending-changes,
                            Out-of-sync, Validating, Configuring, Failed.
                          type: string
                        configType:
                          description: The type of configuration running on the profile.
                            Since profile deployments can configure multiple different
                            settings, configType indicates which type of configuration
                            is currently in progress.
                          type: string
                        controlAction:
                          description: System action to trigger the appropriate workflow.
                            Values -- No_op, ConfigChange, Deploy, Unbind.
                          type: string
                        errorState:
                          description: Indicates a profile's error state. Values --
                            Validation-error (Static validation error), Pre-config-error
                            (Runtime validation error), Config-error (Runtime configuration
                            error).
                          type: string
                        objectType:
                          description: The fully-qualified name of the instantiated,
                            concrete type. The value should be the same as the 'ClassId'
                            property.
                          type: string
                        operState:
                          description: Combined state (configState, and operational
                            state of the associated physical resource) to indicate
                            the current state of the profile. Values -- n/a, Power-off,
                            Pending-changes, Configuring, Ok, Failed.
                          type: string
                      type: object
                    type: array
                  containerRuntimeConfig:
                    description: A reference to a kubernetesContainerRuntimePolicy
                      resource. When the $expand query parameter is specified, the
                      referenced resource is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  createTime:
                    description: The time when this managed object was created.
                    type: string
                  description:
                    description: Description of the profile.
                    type: string
                  domainGroupMoid:
                    description: The DomainGroup ID for this managed object.
                    type: string
                  essentialAddons:
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        addonConfiguration:
                          description: Addon configuration settings that are specific
                            to a single cluster.
                          items:
                            properties:
                              additionalProperties:
                                type: string
                              classId:
                                description: The fully-qualified name of the instantiated,
                                  concrete type. This property is used as a discriminator
                                  to identify the type of the payload when marshaling
                                  and unmarshaling data.
                                type: string
                              installStrategy:
                                description: Addon install strategy to determine whether
                                  an addon is installed if not present. * `None` -
                                  Unspecified install strategy. * `NoAction` - No
                                  install action performed. * `InstallOnly` - Only
                                  install in green field. No action in case of failure
                                  or removal. * `Always` - Attempt install if chart
                                  is not already installed.
                                type: string
                              objectType:
                                description: The fully-qualified name of the instantiated,
                                  concrete type. The value should be the same as the
                                  'ClassId' property.
                                type: string
                              overrideSets:
                                items:
                                  properties:
                                    additionalProperties:
                                      type: string
                                    classId:
                                      description: The fully-qualified name of the
                                        instantiated, concrete type. This property
                                        is used as a discriminator to identify the
                                        type of the payload when marshaling and unmarshaling
                                        data.
                                      type: string
                                    key:
                                      description: Key or property name in a key/value
                                        pair.
                                      type: string
                                    objectType:
                                      description: The fully-qualified name of the
                                        instantiated, concrete type. The value should
                                        be the same as the 'ClassId' property.
                                      type: string
                                    value:
                                      description: Property value in a key/value pair.
                                      type: string
                                  type: object
                                type: array
                              overrides:
                                description: Properties that can be overridden for
                                  an addon.
                                type: string
                              releaseName:
                                description: Name for the helm release.
                                type: string
                              releaseNamespace:
                                description: Namespace for the helm release.
                                type: string
                              upgradeStrategy:
                                description: Addon upgrade strategy to determine whether
                                  an addon configuration is overwritten on upgrade.
                                  * `None` - Unspecified upgrade strategy. * `NoAction`
                                  - This choice enables No upgrades to be performed.
                                  * `UpgradeOnly` - Attempt upgrade if chart or overrides
                                  options change, no action on upgrade failure. *
                                  `ReinstallOnFailure` - Attempt upgrade first. Remove
                                  and install on upgrade failure. * `AlwaysReinstall`
                                  - Always remove older release and reinstall.
                                type: string
                            type: object
                          type: array
                        addonDefinition:
                          description: Addon definition associated with the addon.
                          items:
                            properties:
                              additionalProperties:
                                type: string
                              classId:
                                description: The fully-qualified name of the instantiated,
                                  concrete type. This property is used as a discriminator
                                  to identify the type of the payload when marshaling
                                  and unmarshaling data.
                                type: string
                              moid:
                                description: The Moid of the referenced REST resource.
                                type: string
                              objectType:
                                description: The fully-qualified name of the remote
                                  type referred by this relationship.
                                type: string
                              selector:
                                description: 'An OData $filter expression which describes
                                  the REST resource to be referenced. This field may
                                  be set instead of ''moid'' by clients. 1. If ''moid''
                                  is set this field is ignored. 1. If ''selector''
                                  is set and ''moid'' is empty/absent from the request,
                                  Intersight determines the Moid of the resource matching
                                  the filter expression and populates it in the MoRef
                                  that is part of the object instance being inserted/updated
                                  to fulfill the REST request. An error is returned
                                  if the filter matches zero or more than one REST
                                  resource. An example filter string is: Serial eq
                                  ''3AA8B7T11''.'
                                type: string
                            type: object
                          type: array
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        name:
                          description: Name of addon to be installed on a Kubernetes
                            cluster.
                          type: string
                        objectType:
                          description: The fully-qualified name of the instantiated,
                            concrete type. The value should be the same as the 'ClassId'
                            property.
                          type: string
                      type: object
                    type: array
                  kubeConfig:
                    description: Certificates and keys that are used to access a Kubernetes
                      cluster.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        kubeConfig:
                          description: Kubernetes configuration to connect to the
                            cluster as an system administrator.
                          type: string
                        objectType:
                          description: The fully-qualified name of the instantiated,
                            concrete type. The value should be the same as the 'ClassId'
                            property.
                          type: string
                      type: object
                    type: array
                  loadbalancerIpLeases:
                    description: An array of relationships to ippoolIpLease resources.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  managedMode:
                    description: Management mode for the cluster. In some cases Intersight
                      kubernetes service is not required to provision and manage the
                      management entities and endpoints (for e.g. EKS). In most other
                      cases it will be required to provision and manage these entities
                      and endpoints. * `Provided` - Cluster management entities and
                      endpoints are provided by the infrastructure platform. * `Managed`
                      - Cluster management entities and endpoints are provisioned
                      and managed by Intersight kubernetes service.
                    type: string
                  managementConfig:
                    description: Configuration required for provisioning and management
                      of cluster management entities. Required if 'managedMode' is
                      set to 'Managed'.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        isTacPasswdSet:
                          description: Indicates whether the value of the 'tacPasswd'
                            property has been set.
                          type: boolean
                        loadBalancerCount:
                          description: Number of IP addresses to reserve for load
                            balancer services.
                          format: int64
                          type: integer
                        loadBalancers:
                          items:
                            type: string
                          type: array
                        masterVip:
                          description: VIP for the cluster Kubernetes API server.
                            If this is empty and a cluster IP pool is specified, it
                            will be allocated from the IP pool.
                          type: string
                        objectType:
                          description: The fully-qualified name of the instantiated,
                            concrete type. The value should be the same as the 'ClassId'
                            property.
                          type: string
                        sshKeys:
                          items:
                            type: string
                          type: array
                        sshUser:
                          description: Name of the user to SSH to nodes in a cluster.
                          type: string
                        tacPasswd:
                          description: Hashed password of the TAC user.
                          type: string
                      type: object
                    type: array
                  masterVipLease:
                    description: A reference to a ippoolIpLease resource. When the
                      $expand query parameter is specified, the referenced resource
                      is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  modTime:
                    description: The time when this managed object was last modified.
                    type: string
                  moid:
                    description: The unique identifier of this Managed Object instance.
                    type: string
                  netConfig:
                    description: A reference to a kubernetesNetworkPolicy resource.
                      When the $expand query parameter is specified, the referenced
                      resource is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  nodeGroups:
                    description: An array of relationships to kubernetesNodeGroupProfile
                      resources.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  objectType:
                    description: The fully-qualified name of the instantiated, concrete
                      type. The value should be the same as the 'ClassId' property.
                    type: string
                  organization:
                    description: A reference to a organizationOrganization resource.
                      When the $expand query parameter is specified, the referenced
                      resource is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  owners:
                    items:
                      type: string
                    type: array
                  parent:
                    description: A reference to a moBaseMo resource. When the $expand
                      query parameter is specified, the referenced resource is returned
                      inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  parentSolutionProfile:
                    description: A reference to a moBaseMo resource. When the $expand
                      query parameter is specified, the referenced resource is returned
                      inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  permissionResources:
                    description: An array of relationships to moBaseMo resources.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  policyBucket:
                    description: An array of relationships to policyAbstractPolicy
                      resources.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  sharedScope:
                    description: Intersight provides pre-built workflows, tasks and
                      policies to end users through global catalogs. Objects that
                      are made available through global catalogs are said to have
                      a 'shared' ownership. Shared objects are either made globally
                      available to all end users or restricted to end users based
                      on their license entitlement. Users can use this property to
                      differentiate the scope (global or a specific license tier)
                      to which a shared MO belongs.
                    type: string
                  srcTemplate:
                    description: A reference to a policyAbstractProfile resource.
                      When the $expand query parameter is specified, the referenced
                      resource is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  status:
                    description: Status of the Kubernetes cluster and its nodes. *
                      `Undeployed` - The cluster is undeployed. * `Configuring` -
                      The cluster is being configured. * `Deploying` - The cluster
                      is being deployed. * `Undeploying` - The cluster is being undeployed.
                      * `DeployFailedTerminal` - The cluster deployment failed terminally
                      and can not be recovered. * `DeployFailed` - The cluster deployment
                      failed. * `Upgrading` - The cluster is being upgraded. * `Deleting`
                      - The cluster is being deleted. * `DeleteFailed` - The cluster
                      delete failed. * `Ready` - The cluster is ready for use. * `Active`
                      - The cluster is being active. * `Shutdown` - All the nodes
                      in the cluster are powered off. * `Terminated` - The cluster
                      is terminated. * `Deployed` - The cluster is deployed. The cluster
                      may not yet be ready for use. * `UndeployFailed` - The cluster
                      undeploy action failed. * `NotReady` - The cluster is created
                      and some nodes are not ready.
                    type: string
                  sysConfig:
                    description: A reference to a kubernetesSysConfigPolicy resource.
                      When the $expand query parameter is specified, the referenced
                      resource is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  tags:
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        key:
                          description: The string representation of a tag key.
                          type: string
                        value:
                          description: The string representation of a tag value.
                          type: string
                      type: object
                    type: array
                  trustedRegistries:
                    description: A reference to a kubernetesTrustedRegistriesPolicy
                      resource. When the $expand query parameter is specified, the
                      referenced resource is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  type:
                    description: Defines the type of the profile. Accepted values
                      are instance or template. * `instance` - The profile defines
                      the configuration for a specific instance of a target.
                    type: string
                  versionContext:
                    description: The versioning info for this managed object.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        interestedMos:
                          items:
                            properties:
                              additionalProperties:
                                type: string
                              classId:
                                description: The fully-qualified name of the instantiated,
                                  concrete type. This property is used as a discriminator
                                  to identify the type of the payload when marshaling
                                  and unmarshaling data.
                                type: string
                              moid:
                                description: The Moid of the referenced REST resource.
                                type: string
                              objectType:
                                description: The fully-qualified name of the remote
                                  type referred by this relationship.
                                type: string
                              selector:
                                description: 'An OData $filter expression which describes
                                  the REST resource to be referenced. This field may
                                  be set instead of ''moid'' by clients. 1. If ''moid''
                                  is set this field is ignored. 1. If ''selector''
                                  is set and ''moid'' is empty/absent from the request,
                                  Intersight determines the Moid of the resource matching
                                  the filter expression and populates it in the MoRef
                                  that is part of the object instance being inserted/updated
                                  to fulfill the REST request. An error is returned
                                  if the filter matches zero or more than one REST
                                  resource. An example filter string is: Serial eq
                                  ''3AA8B7T11''.'
                                type: string
                            type: object
                          type: array
                        nrVersion:
                          description: The version of the Managed Object, e.g. an
                            incrementing number or a hash id.
                          type: string
                        objectType:
                          description: The fully-qualified name of the instantiated,
                            concrete type. The value should be the same as the 'ClassId'
                            property.
                          type: string
                        refMo:
                          description: A reference to the original Managed Object.
                          items:
                            properties:
                              additionalProperties:
                                type: string
                              classId:
                                description: The fully-qualified name of the instantiated,
                                  concrete type. This property is used as a discriminator
                                  to identify the type of the payload when marshaling
                                  and unmarshaling data.
                                type: string
                              moid:
                                description: The Moid of the referenced REST resource.
                                type: string
                              objectType:
                                description: The fully-qualified name of the remote
                                  type referred by this relationship.
                                type: string
                              selector:
                                description: 'An OData $filter expression which describes
                                  the REST resource to be referenced. This field may
                                  be set instead of ''moid'' by clients. 1. If ''moid''
                                  is set this field is ignored. 1. If ''selector''
                                  is set and ''moid'' is empty/absent from the request,
                                  Intersight determines the Moid of the resource matching
                                  the filter expression and populates it in the MoRef
                                  that is part of the object instance being inserted/updated
                                  to fulfill the REST request. An error is returned
                                  if the filter matches zero or more than one REST
                                  resource. An example filter string is: Serial eq
                                  ''3AA8B7T11''.'
                                type: string
                            type: object
                          type: array
                        timestamp:
                          description: The time this versioned Managed Object was
                            created.
                          type: string
                        versionType:
                          description: Specifies type of version. Currently the only
                            supported value is "Configured" that is used to keep track
                            of snapshots of policies and profiles that are intended
                            to be configured to target endpoints. * `Modified` - Version
                            created every time an object is modified. * `Configured`
                            - Version created every time an object is configured to
                            the service profile. * `Deployed` - Version created for
                            objects related to a service profile when it is deployed.
                          type: string
                      type: object
                    type: array
                  waitForCompletion:
                    description: This model object can trigger workflows. Use this
                      option to wait for all running workflows to reach a complete
                      state.
                    type: boolean
                  workflowInfo:
                    description: A reference to a workflowWorkflowInfo resource. When
                      the $expand query parameter is specified, the referenced resource
                      is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                type: object
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be
                  used to create, observe, update, and delete this managed resource.
                  Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ClusterProfileStatus defines the observed state of ClusterProfile.
            properties:
              atProvider:
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
