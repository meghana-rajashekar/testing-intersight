
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.2
  creationTimestamp: null
  name: ethifs.vnic.intersight.jet.crossplane.io
spec:
  group: vnic.intersight.jet.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - intersightjet
    kind: EthIf
    listKind: EthIfList
    plural: ethifs
    singular: ethif
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EthIf is the Schema for the EthIfs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EthIfSpec defines the desired state of EthIf
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource.
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accountMoid:
                    description: The Account ID for this managed object.
                    type: string
                  additionalProperties:
                    type: string
                  ancestors:
                    description: An array of relationships to moBaseMo resources.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  cdn:
                    description: Consistent Device Naming configuration for the virtual
                      NIC.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        nrSource:
                          description: Source of the CDN. It can either be user specified
                            or be the same as the vNIC name. * `vnic` - Source of
                            the CDN is the same as the vNIC name. * `user` - Source
                            of the CDN is specified by the user.
                          type: string
                        objectType:
                          description: The fully-qualified name of the instantiated,
                            concrete type. The value should be the same as the 'ClassId'
                            property.
                          type: string
                        value:
                          description: The CDN value entered in case of user defined
                            mode.
                          type: string
                      type: object
                    type: array
                  classId:
                    description: The fully-qualified name of the instantiated, concrete
                      type. This property is used as a discriminator to identify the
                      type of the payload when marshaling and unmarshaling data.
                    type: string
                  createTime:
                    description: The time when this managed object was created.
                    type: string
                  domainGroupMoid:
                    description: The DomainGroup ID for this managed object.
                    type: string
                  ethAdapterPolicy:
                    description: A reference to a vnicEthAdapterPolicy resource. When
                      the $expand query parameter is specified, the referenced resource
                      is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  ethNetworkPolicy:
                    description: A reference to a vnicEthNetworkPolicy resource. When
                      the $expand query parameter is specified, the referenced resource
                      is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  ethQosPolicy:
                    description: A reference to a vnicEthQosPolicy resource. When
                      the $expand query parameter is specified, the referenced resource
                      is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  fabricEthNetworkControlPolicy:
                    description: A reference to a fabricEthNetworkControlPolicy resource.
                      When the $expand query parameter is specified, the referenced
                      resource is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  fabricEthNetworkGroupPolicy:
                    description: An array of relationships to fabricEthNetworkGroupPolicy
                      resources.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  failoverEnabled:
                    description: Enabling failover ensures that traffic from the vNIC
                      automatically fails over to the secondary Fabric Interconnect,
                      in case the specified Fabric Interconnect path goes down. Failover
                      applies only to Cisco VICs that are connected to a Fabric Interconnect
                      cluster.
                    type: boolean
                  ipLease:
                    description: A reference to a ippoolIpLease resource. When the
                      $expand query parameter is specified, the referenced resource
                      is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  iscsiBootPolicy:
                    description: A reference to a vnicIscsiBootPolicy resource. When
                      the $expand query parameter is specified, the referenced resource
                      is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  iscsiIpV4AddressAllocationType:
                    description: Static/Pool/DHCP Type of IP address allocated to
                      the vNIC. It is derived from iSCSI boot policy IP Address type.
                      * `None` - Type indicates that there is no IP associated to
                      an vnic. * `DHCP` - The IP address is assigned using DHCP, if
                      available. * `Static` - Static IPv4 address is assigned to the
                      iSCSI boot interface based on the information entered in this
                      area. * `Pool` - An IPv4 address is assigned to the iSCSI boot
                      interface from the management IP address pool.
                    type: string
                  iscsiIpV4Config:
                    description: IPV4 configurations such as Netmask, Gateway and
                      DNS for iSCSI vNIC.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        gateway:
                          description: IP address of the default IPv4 gateway.
                          type: string
                        netmask:
                          description: A subnet mask is a 32-bit number that masks
                            an IP address and divides the IP address into network
                            address and host address.
                          type: string
                        objectType:
                          description: The fully-qualified name of the instantiated,
                            concrete type. The value should be the same as the 'ClassId'
                            property.
                          type: string
                        primaryDns:
                          description: IP Address of the primary Domain Name System
                            (DNS) server.
                          type: string
                        secondaryDns:
                          description: IP Address of the secondary Domain Name System
                            (DNS) server.
                          type: string
                      type: object
                    type: array
                  iscsiIpv4Address:
                    description: IP address associated to the vNIC.
                    type: string
                  lanConnectivityPolicy:
                    description: A reference to a vnicLanConnectivityPolicy resource.
                      When the $expand query parameter is specified, the referenced
                      resource is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  lcpVnic:
                    description: A reference to a vnicEthIf resource. When the $expand
                      query parameter is specified, the referenced resource is returned
                      inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  macAddress:
                    description: The MAC address that is assigned to the vNIC based
                      on the MAC pool that has been assigned to the LAN Connectivity
                      Policy.
                    type: string
                  macAddressType:
                    description: Type of allocation selected to assign a MAC address
                      for the vnic. * `POOL` - The user selects a pool from which
                      the mac/wwn address will be leased for the Virtual Interface.
                      * `STATIC` - The user assigns a static mac/wwn address for the
                      Virtual Interface.
                    type: string
                  macLease:
                    description: A reference to a macpoolLease resource. When the
                      $expand query parameter is specified, the referenced resource
                      is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  macPool:
                    description: A reference to a macpoolPool resource. When the $expand
                      query parameter is specified, the referenced resource is returned
                      inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  modTime:
                    description: The time when this managed object was last modified.
                    type: string
                  moid:
                    description: The unique identifier of this Managed Object instance.
                    type: string
                  objectType:
                    description: The fully-qualified name of the instantiated, concrete
                      type. The value should be the same as the 'ClassId' property.
                    type: string
                  order:
                    description: The order in which the virtual interface is brought
                      up. The order assigned to an interface should be unique for
                      all the Ethernet and Fibre-Channel interfaces on each PCI link
                      on a VIC adapter. The maximum value of PCI order is limited
                      by the number of virtual interfaces (Ethernet and Fibre-Channel)
                      on each PCI link on a VIC adapter. All VIC adapters have a single
                      PCI link except VIC 1385 which has two.
                    format: int64
                    type: integer
                  owners:
                    items:
                      type: string
                    type: array
                  parent:
                    description: A reference to a moBaseMo resource. When the $expand
                      query parameter is specified, the referenced resource is returned
                      inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  permissionResources:
                    description: An array of relationships to moBaseMo resources.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  placement:
                    description: Placement Settings for the virtual interface.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        id:
                          description: PCIe Slot where the VIC adapter is installed.
                            Supported values are (1-15) and MLOM.
                          type: string
                        objectType:
                          description: The fully-qualified name of the instantiated,
                            concrete type. The value should be the same as the 'ClassId'
                            property.
                          type: string
                        pciLink:
                          description: The PCI Link used as transport for the virtual
                            interface. All VIC adapters have a single PCI link except
                            VIC 1385 which has two.
                          format: int64
                          type: integer
                        switchId:
                          description: The fabric port to which the vNICs will be
                            associated. * `None` - Fabric Id is not set to either
                            A or B for the standalone case where the server is not
                            connected to Fabric Interconnects. The value 'None' should
                            be used. * `A` - Fabric A of the FI cluster. * `B` - Fabric
                            B of the FI cluster.
                          type: string
                        uplink:
                          description: Adapter port on which the virtual interface
                            will be created.
                          format: int64
                          type: integer
                      type: object
                    type: array
                  profile:
                    description: A reference to a policyAbstractConfigProfile resource.
                      When the $expand query parameter is specified, the referenced
                      resource is returned inline.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  sharedScope:
                    description: Intersight provides pre-built workflows, tasks and
                      policies to end users through global catalogs. Objects that
                      are made available through global catalogs are said to have
                      a 'shared' ownership. Shared objects are either made globally
                      available to all end users or restricted to end users based
                      on their license entitlement. Users can use this property to
                      differentiate the scope (global or a specific license tier)
                      to which a shared MO belongs.
                    type: string
                  spVnics:
                    description: An array of relationships to vnicEthIf resources.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        moid:
                          description: The Moid of the referenced REST resource.
                          type: string
                        objectType:
                          description: The fully-qualified name of the remote type
                            referred by this relationship.
                          type: string
                        selector:
                          description: 'An OData $filter expression which describes
                            the REST resource to be referenced. This field may be
                            set instead of ''moid'' by clients. 1. If ''moid'' is
                            set this field is ignored. 1. If ''selector'' is set and
                            ''moid'' is empty/absent from the request, Intersight
                            determines the Moid of the resource matching the filter
                            expression and populates it in the MoRef that is part
                            of the object instance being inserted/updated to fulfill
                            the REST request. An error is returned if the filter matches
                            zero or more than one REST resource. An example filter
                            string is: Serial eq ''3AA8B7T11''.'
                          type: string
                      type: object
                    type: array
                  standbyVifId:
                    description: The Standby VIF Id is applicable for failover enabled
                      vNICS. It should be the same as the channel number of the standby
                      vethernet created on switch in order to set up the standby data
                      path.
                    format: int64
                    type: integer
                  staticMacAddress:
                    description: The MAC address must be in hexadecimal format xx:xx:xx:xx:xx:xx.
                      To ensure uniqueness of MACs in the LAN fabric, you are strongly
                      encouraged to use the following MAC prefix 00:25:B5:xx:xx:xx.
                    type: string
                  tags:
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        key:
                          description: The string representation of a tag key.
                          type: string
                        value:
                          description: The string representation of a tag value.
                          type: string
                      type: object
                    type: array
                  usnicSettings:
                    description: User Space NIC Settings that enable low-latency and
                      higher throughput by bypassing the kernel layer when sending/receiving
                      packets.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        cos:
                          description: Class of Service to be used for traffic on
                            the usNIC.
                          format: int64
                          type: integer
                        nrCount:
                          description: Number of usNIC interfaces to be created.
                          format: int64
                          type: integer
                        objectType:
                          description: The fully-qualified name of the instantiated,
                            concrete type. The value should be the same as the 'ClassId'
                            property.
                          type: string
                        usnicAdapterPolicy:
                          description: Ethernet Adapter policy to be associated with
                            the usNICs.
                          type: string
                      type: object
                    type: array
                  versionContext:
                    description: The versioning info for this managed object.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        interestedMos:
                          items:
                            properties:
                              additionalProperties:
                                type: string
                              classId:
                                description: The fully-qualified name of the instantiated,
                                  concrete type. This property is used as a discriminator
                                  to identify the type of the payload when marshaling
                                  and unmarshaling data.
                                type: string
                              moid:
                                description: The Moid of the referenced REST resource.
                                type: string
                              objectType:
                                description: The fully-qualified name of the remote
                                  type referred by this relationship.
                                type: string
                              selector:
                                description: 'An OData $filter expression which describes
                                  the REST resource to be referenced. This field may
                                  be set instead of ''moid'' by clients. 1. If ''moid''
                                  is set this field is ignored. 1. If ''selector''
                                  is set and ''moid'' is empty/absent from the request,
                                  Intersight determines the Moid of the resource matching
                                  the filter expression and populates it in the MoRef
                                  that is part of the object instance being inserted/updated
                                  to fulfill the REST request. An error is returned
                                  if the filter matches zero or more than one REST
                                  resource. An example filter string is: Serial eq
                                  ''3AA8B7T11''.'
                                type: string
                            type: object
                          type: array
                        nrVersion:
                          description: The version of the Managed Object, e.g. an
                            incrementing number or a hash id.
                          type: string
                        objectType:
                          description: The fully-qualified name of the instantiated,
                            concrete type. The value should be the same as the 'ClassId'
                            property.
                          type: string
                        refMo:
                          description: A reference to the original Managed Object.
                          items:
                            properties:
                              additionalProperties:
                                type: string
                              classId:
                                description: The fully-qualified name of the instantiated,
                                  concrete type. This property is used as a discriminator
                                  to identify the type of the payload when marshaling
                                  and unmarshaling data.
                                type: string
                              moid:
                                description: The Moid of the referenced REST resource.
                                type: string
                              objectType:
                                description: The fully-qualified name of the remote
                                  type referred by this relationship.
                                type: string
                              selector:
                                description: 'An OData $filter expression which describes
                                  the REST resource to be referenced. This field may
                                  be set instead of ''moid'' by clients. 1. If ''moid''
                                  is set this field is ignored. 1. If ''selector''
                                  is set and ''moid'' is empty/absent from the request,
                                  Intersight determines the Moid of the resource matching
                                  the filter expression and populates it in the MoRef
                                  that is part of the object instance being inserted/updated
                                  to fulfill the REST request. An error is returned
                                  if the filter matches zero or more than one REST
                                  resource. An example filter string is: Serial eq
                                  ''3AA8B7T11''.'
                                type: string
                            type: object
                          type: array
                        timestamp:
                          description: The time this versioned Managed Object was
                            created.
                          type: string
                        versionType:
                          description: Specifies type of version. Currently the only
                            supported value is "Configured" that is used to keep track
                            of snapshots of policies and profiles that are intended
                            to be configured to target endpoints. * `Modified` - Version
                            created every time an object is modified. * `Configured`
                            - Version created every time an object is configured to
                            the service profile. * `Deployed` - Version created for
                            objects related to a service profile when it is deployed.
                          type: string
                      type: object
                    type: array
                  vifId:
                    description: The Vif Id should be same as the channel number of
                      the vethernet created on switch in order to set up the data
                      path. The property is applicable only for FI attached servers
                      where a vethernet is created on the switch for every vNIC.
                    format: int64
                    type: integer
                  vmqSettings:
                    description: Virtual Machine Queue Settings for the virtual interface
                      that allow efficient transfer of network traffic to the guest
                      OS.
                    items:
                      properties:
                        additionalProperties:
                          type: string
                        classId:
                          description: The fully-qualified name of the instantiated,
                            concrete type. This property is used as a discriminator
                            to identify the type of the payload when marshaling and
                            unmarshaling data.
                          type: string
                        enabled:
                          description: Enables VMQ feature on the virtual interface.
                          type: boolean
                        multiQueueSupport:
                          description: Enables Virtual Machine Multi-Queue feature
                            on the virtual interface. VMMQ allows configuration of
                            multiple I/O queues for a single VM and thus distributes
                            traffic across multiple CPU cores in a VM.
                          type: boolean
                        numInterrupts:
                          description: The number of interrupt resources to be allocated.
                            Recommended value is the number of CPU threads or logical
                            processors available in the server.
                          format: int64
                          type: integer
                        numSubVnics:
                          description: The number of sub vNICs to be created.
                          format: int64
                          type: integer
                        numVmqs:
                          description: The number of hardware Virtual Machine Queues
                            to be allocated. The number of VMQs per adapter must be
                            one more than the maximum number of VM NICs.
                          format: int64
                          type: integer
                        objectType:
                          description: The fully-qualified name of the instantiated,
                            concrete type. The value should be the same as the 'ClassId'
                            property.
                          type: string
                        vmmqAdapterPolicy:
                          description: Ethernet Adapter policy to be associated with
                            the Sub vNICs. The Transmit Queue and Receive Queue resource
                            value of VMMQ adapter policy should be greater than or
                            equal to the configured number of sub vNICs.
                          type: string
                      type: object
                    type: array
                type: object
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be
                  used to create, observe, update, and delete this managed resource.
                  Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: EthIfStatus defines the observed state of EthIf.
            properties:
              atProvider:
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
